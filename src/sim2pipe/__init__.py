"""sim2pipe — evaluate synthetic IMU through the main project's own pipeline.

Design: docs/sim2pipe_design.md. Boundary: file-level export + subprocess
into the main repo's CLI; no main-project code is vendored here.
"""
