# Experiments

Use this directory for experiment-specific notes, simple configs, ablation records, and comparison setups.

Keep experiment descriptions lightweight. If a setting is reusable across runs, move the logic into `src/` and call it from `scripts/`.
